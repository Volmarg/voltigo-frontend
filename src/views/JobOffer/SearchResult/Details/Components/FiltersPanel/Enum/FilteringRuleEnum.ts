/**
 * @description must be in sync with backend `KeywordsFilteringRule`
 */
enum KEYWORD_FILTERING_RULE {
    "NONE"
}

/**
 * @description must be in sync with backend "PostedRule"
 */
enum POSTED_FILTERING_RULE {
    "NONE"
}

export {KEYWORD_FILTERING_RULE, POSTED_FILTERING_RULE};