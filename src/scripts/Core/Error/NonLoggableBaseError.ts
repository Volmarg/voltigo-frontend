import BaseError from "@/scripts/Core/Error/BaseError";

/**
 * @description this is basically a base error but it indicates that nothing should / will be logged once this
 *              error happens
 */
export default class NonLoggableBaseError extends BaseError {

}