export type Account = {
    maxParallelJobSearches: number | null,
    maxSearchedKeywords: number | null,
    maxPointsAllowed: number | null,
    canGeneratePdfFromTemplate: boolean,
    hasBoughtAnyPoints: boolean,
}