/**
 * @description an listener defining base structure of the event listeners
 */
export default interface ListenerInterface
{
    /**
     * @description start listening for the event
     */
    startListening(): void;

}
